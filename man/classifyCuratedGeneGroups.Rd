% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classifyCuratedGeneGroups.R
\name{classifyCuratedGeneGroups}
\alias{classifyCuratedGeneGroups}
\title{Classify genes into curated HGNC gene groups}
\usage{
classifyCuratedGeneGroups(
  ensemblGeneIds,
  organism,
  hgnc = NULL,
  jax = NULL,
  mgi = NULL
)
}
\arguments{
\item{ensemblGeneIds}{\code{character}.
Ensembl gene identifiers to classify.}

\item{organism}{\code{character(1)}.
Latin organism name. Only \code{"Homo sapiens"} and \code{"Mus musculus"} are
supported.}

\item{hgnc}{\code{Hgnc} or \code{NULL}.
HGNC reference dataset. Downloaded via \code{Hgnc()} if \code{NULL}.}

\item{jax}{\code{JaxHumanToMouse} or \code{NULL}.
JAX human-to-mouse ortholog dataset. Downloaded via
\code{JaxHumanToMouse(unique = FALSE)} if \code{NULL}. Ignored for
\code{"Homo sapiens"}.}

\item{mgi}{\code{Mgi} or \code{NULL}.
MGI reference dataset. Downloaded via \code{Mgi()} if \code{NULL}. Ignored for
\code{"Homo sapiens"}.}
}
\value{
\code{list}.
Named list keyed by \code{ensemblGeneIds}, each element a \code{character} vector
of curated tags (empty if none).
}
\description{
Classify genes into curated HGNC gene groups
}
\details{
Tags each gene \code{"riboCyto"} (cytoplasmic ribosomal protein), \code{"riboMito"}
(mitochondrial ribosomal protein), \code{"hemoglobin"} (hemoglobin subunit), or
none of these. Tags are sourced from HGNC's own curated \code{geneGroupId}
assignments, never a symbol regex; a gene not in any curated group gets an
empty \code{character} vector, not an omitted list element.

For Mus musculus, human HGNC groups are propagated via a fully
identifier-based chain (HGNC \code{hgncId} -> JAX ortholog \code{mouseMgiId} -> MGI
\code{ensemblGeneId}), with no gene-symbol matching at any step.

This is deliberately independent of \code{broadClass} (see the internal
\code{.addBroadClass()} in \code{internal-GenomicRanges.R}): \code{broadClass} is
single-valued and every ribosomal/hemoglobin gene already has a value
from it (\code{"coding"}, \code{"pseudo"}, etc). Do not fold these tags into
\code{broadClass}.

A sibling Python package carries the identical group-ID map and function
as a hand-ported twin; a change here must land there too, in the same
release.
}
\note{
Updated 2026-09-24.
}
\examples{
tags <- classifyCuratedGeneGroups(
    ensemblGeneIds = "ENSG00000244734",
    organism = "Homo sapiens"
)
print(tags[["ENSG00000244734"]]) # HBB
}
